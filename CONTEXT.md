# 文旅 AIGC 案例站

面向文旅客户的 AIGC 视频案例展示站，用于展示作品、说明服务能力并引导咨询转化。当前正在从单页站扩展为多页站，重点是 SEO/GEO（搜索引擎与生成式引擎优化）。

## Language

**Slug**:
案例的 SEO 友好 URL 标识，由标题自动转写为拼音短横线格式生成（如“苏东坡的荔枝狂想”→ `su-dong-po-de-li-zhi-kuang-xiang`），不由人工维护。与 `id`（UUID，内部主键）并存：`id` 用于数据库关联与后台操作，`slug` 只用于案例详情页 URL。
_Avoid_: id（当指代 URL 路径时不要用 id，两者用途不同）

**摘要（Summary）**:
案例列表卡片上展示的一两句短介绍，对应现有 `summary` 字段。只用于列表页/首页卡片，不是详情页正文。
_Avoid_: 简介（与“详情正文”混用时容易指代不清，需要明确是列表用的短摘要）

**详情正文（Case Body）**:
案例详情页专属的长文本内容字段（新增），承载完整的项目背景、创作思路等，是详情页 SEO/GEO 文本密度的主要来源。与 `summary` 是两个不同字段，互不替代。
_Avoid_: 简介、summary（这两个词专指列表用的短摘要）
