# 文旅 AI 视频案例官网动效交互说明

## 1. 页面定位

这是一个面向文旅行业的 AI 视频案例展示官网，页面只保留两个核心内容：

- 案例展示
- 关于我们 / 联系合作

案例分为两类：

- 宣传片
- 广告片

整体风格参考当前确认的页面效果：深色电影感背景、大图视觉、右侧案例卡片轮播、金色点缀、轻微悬浮感。页面不做复杂功能，重点是让客户一进来就感受到“高级、影像化、文旅行业定制”。

---

## 2. 页面整体动效

### 2.1 首屏载入动效

页面首次进入时，按顺序出现：

1. 背景大图先淡入，时长约 `800ms`
2. 深色遮罩同步淡入，保证文字可读
3. 顶部导航从上方轻微下移并淡入
4. 左侧主标题从左侧 `24px` 滑入并淡入
5. 副标题、描述文字、按钮依次淡入
6. 右侧案例卡片从右侧滑入，卡片之间有 `80ms` 左右的错位延迟
7. 底部进度条、箭头、页码最后淡入

整体进入节奏要慢一点，不要弹跳，不要夸张。建议使用 `ease-out` 或 `cubic-bezier(0.22, 1, 0.36, 1)`。

---

## 3. 背景切换动效

### 3.1 背景与当前案例联动

当前选中的案例决定页面背景图。

例如：

- 当前案例是“增城文旅”，背景显示增城相关山水、人文、城市夜景
- 当前案例是“南阳汉画馆”，背景显示博物馆、汉画像石、展厅光影
- 当前案例切到“更多案例”，背景显示更抽象的山水、长城、云海等氛围图

### 3.2 切换方式

背景不要直接闪切，要做交叉淡入：

- 旧背景保持不动并逐渐透明
- 新背景从透明变为可见
- 时长：`700ms - 1000ms`
- 新背景可以有极轻微的缩放，从 `scale(1.04)` 过渡到 `scale(1)`

这样能产生电影镜头切换感。

### 3.3 背景遮罩

背景上始终叠加深色遮罩：

- 左侧遮罩略深，保证标题清晰
- 右侧遮罩略浅，保证卡片图片有层次
- 底部加轻微黑色渐变，承接进度条和页码

可以使用三层遮罩：

1. 全局黑色透明层：`rgba(0,0,0,0.35)`
2. 左侧线性渐变：左深右浅
3. 底部线性渐变：下深上浅

---

## 4. 案例卡片轮播交互

### 4.1 卡片布局

右侧是横向案例卡片组。

建议显示：

- 当前主案例：完整大卡片
- 第二个案例：完整大卡片
- 第三个案例：窄卡片或半透明卡片
- 第四个案例：露出一部分，暗示可继续滑动

卡片之间保持较大间距，不要太密集。

### 4.2 当前卡片状态

当前选中的卡片需要更突出：

- 尺寸略大或亮度更高
- 边框更明显，使用半透明浅色边框
- 图片亮度更高
- 内部文案完全显示
- 播放按钮显示在卡片中部偏左位置

非当前卡片：

- 图片降低亮度
- 文字仍可读，但不要抢主卡片
- 可以有轻微透明度或深色遮罩

### 4.3 点击卡片

点击任意案例卡片时：

1. 该卡片变为当前案例
2. 页面背景切换为该案例背景
3. 左侧标题、描述文案切换为对应内容
4. 底部页码和进度条同步变化
5. 卡片列表横向滑动，把当前卡片移动到主要展示位置

动效时长建议：

- 卡片滑动：`600ms`
- 文案切换：`300ms` 淡出 + `300ms` 淡入
- 背景切换：`800ms`

### 4.4 左右箭头

底部左右箭头控制案例切换。

点击右箭头：

- 切换到下一个案例
- 如果已经是最后一个，循环回第一个

点击左箭头：

- 切换到上一个案例
- 如果已经是第一个，循环到最后一个

箭头 hover 效果：

- 圆形边框变亮
- 箭头轻微移动 `2px`
- 背景出现淡金色透明填充

---

## 5. 分类切换交互：宣传片 / 广告片

### 5.1 分类位置

分类放在右侧案例区域上方，样式为文字 Tab：

- 宣传片
- 广告片

当前分类使用金色文字，下方有一条金色短线。

### 5.2 切换动效

点击分类时：

1. 当前分类下划线滑动到目标分类
2. 案例卡片整体淡出并轻微下移
3. 切换数据
4. 新分类卡片淡入并轻微上移
5. 默认选中该分类的第一个案例
6. 背景、左侧文案、页码、进度条同步更新

建议时长：

- Tab 下划线移动：`300ms`
- 卡片组切换：`450ms`
- 背景切换：`800ms`

### 5.3 分类内容示例

宣传片：

- 增城文旅
- 南阳汉画馆
- 更多案例
- 即将上线

广告片：

- 景区活动广告
- 城市品牌广告
- 博物馆展览广告
- 即将上线

---

## 6. 左侧文案动效

### 6.1 标题切换

左侧大标题跟随当前案例或当前分类变化。

首页默认标题：

```text
AI 文旅视频案例
```

副标题：

```text
用影像讲好地方故事
```

当切换到具体案例时，可以显示案例名称：

```text
增城文旅
山水入画 · 岭南增城
```

```text
南阳汉画馆
汉韵千年 · 文化永恒
```

### 6.2 文案切换方式

文案切换不要整块闪烁。推荐：

1. 旧文案向下 `8px` 并淡出
2. 新文案从上方 `8px` 淡入
3. 标题、二级标题、描述分层延迟出现

建议：

- 大标题：`300ms`
- 副标题：延迟 `80ms`
- 描述：延迟 `160ms`
- 按钮：延迟 `220ms`

---

## 7. 播放按钮交互

### 7.1 首页主按钮

左侧按钮为“观看作品集”。

点击后可以：

- 打开视频弹窗
- 或滚动 / 切换到当前案例详情

当前阶段建议先做视频弹窗。

### 7.2 卡片播放按钮

每个案例卡片上有一个播放按钮。

点击播放按钮时：

1. 页面背景不跳转
2. 弹出全屏或居中视频播放器
3. 背景页面加模糊和暗化
4. 视频弹窗从 `scale(0.96)` 到 `scale(1)` 淡入
5. 右上角显示关闭按钮

关闭时：

- 视频暂停
- 弹窗淡出
- 页面恢复原状态

### 7.3 播放按钮 hover

播放按钮 hover 时：

- 圆形按钮轻微放大到 `scale(1.06)`
- 金色背景更亮
- 外圈出现轻微扩散光晕

---

## 8. 进度条与页码联动

### 8.1 页码

右下角显示当前案例序号：

```text
01 / 04
```

当前序号使用金色或浅金色，大号显示；总数使用灰色小号。

切换案例时：

- 旧数字向下淡出
- 新数字从上方淡入
- 时长 `300ms`

### 8.2 进度条

底部中间是一条横向进度线。

规则：

- 进度条总长度固定
- 按案例数量分成若干段
- 当前段使用金色
- 未激活段使用深灰色或半透明白色

切换案例时，金色段滑动到对应位置，不要瞬间跳变。

---

## 9. 顶部导航交互

顶部只保留三个入口：

- 案例展示
- 关于我们
- 联系合作

三项样式保持一致，不要把“联系合作”做成大按钮。它和“关于我们”保持同样的文字导航样式，中间用细竖线分隔。

导航 hover：

- 文字颜色从灰白变为浅金色
- 下方出现极短的金色细线
- 时长 `200ms`

点击行为：

- 案例展示：回到案例轮播区域 / 重置到首屏
- 关于我们：滚动到左下角关于我们模块，或展开关于我们内容
- 联系合作：打开联系合作弹窗，或者滚动到联系合作模块

---

## 10. 关于我们 / 联系合作模块交互

### 10.1 关于我们展示方式

左下角保留一个轻量“关于我们”信息区，不要做成独立大板块。

结构：

```text
关于我们
我们是一支深耕文旅领域的 AI 影像创作团队，
用技术与审美连接文化与未来，让每一处风景都被世界记住。
了解更多 →
```

### 10.2 联系合作

“联系合作”与“关于我们”保持同一种导航文字风格。

点击“联系合作”可以打开一个轻量弹窗：

- 标题：联系合作
- 文案：告诉我们你的项目需求，我们将为你提供文旅影像解决方案
- 表单字段：姓名 / 手机号 / 项目类型 / 需求描述
- 按钮：提交需求

弹窗动效：

- 背景暗化
- 弹窗从右侧轻微滑入或从中心淡入
- 时长 `350ms`

---

## 11. 鼠标悬浮细节

### 11.1 案例卡片 hover

卡片 hover 时：

- 图片轻微放大 `scale(1.04)`
- 卡片整体上移 `-6px`
- 边框变亮
- 底部渐变遮罩略增强
- “查看案例 →”箭头向右移动 `4px`

### 11.2 背景轻微视差

鼠标在页面上移动时，背景可以做非常轻微的视差：

- 背景图根据鼠标位置移动 `±8px`
- 卡片组移动 `±4px`
- 左侧文字不移动或只移动 `±2px`

注意：视差要非常轻，不要影响阅读。

---

## 12. 自动播放轮播

页面可以自动轮播案例。

建议规则：

- 每 `5s - 7s` 自动切换到下一个案例
- 用户鼠标 hover 到卡片区域时暂停自动轮播
- 用户点击箭头或卡片后，重置自动轮播计时
- 打开视频弹窗时暂停自动轮播
- 关闭视频弹窗后恢复自动轮播

---

## 13. 移动端交互

移动端不需要完全复刻桌面横向布局。

建议：

- 顶部导航简化为菜单按钮
- 左侧主标题变成顶部标题区
- 案例卡片变成横向滑动卡片
- 分类 Tab 保持在卡片上方
- 页码和进度条放在卡片下方
- 关于我们放在案例下方

移动端手势：

- 支持左右滑动切换案例
- 滑动结束后自动吸附到最近卡片
- 当前卡片居中显示

---

## 14. 推荐实现方式

建议使用：

- Next.js / React
- Tailwind CSS
- Framer Motion
- Swiper 或自定义 Carousel

核心状态：

```ts
activeCategory: 'promo' | 'ad'
activeIndex: number
isVideoOpen: boolean
```

每个案例的数据结构：

```ts
interface CaseItem {
  id: string
  category: 'promo' | 'ad'
  title: string
  subtitle: string
  description: string
  cover: string
  background: string
  videoUrl?: string
}
```

页面所有视觉内容都应该由当前 `activeCategory + activeIndex` 驱动，包括：

- 背景图
- 左侧标题
- 左侧描述
- 当前卡片
- 页码
- 进度条
- 视频播放地址

---

## 15. 动效验收标准

开发完成后按以下标准验收：

1. 首屏进入有电影感，不是普通网页直接出现
2. 切换案例时，背景、文案、卡片、页码、进度条全部联动
3. 宣传片 / 广告片切换时，有清晰但克制的过渡
4. 卡片 hover 有高级感，不要花哨
5. 播放弹窗打开和关闭顺滑
6. 自动轮播不会打断用户操作
7. 移动端可以正常横向滑动案例
8. 页面只突出案例展示和关于我们，不添加多余模块
9. 整体字体要克制、现代、协调，避免使用过于书法化或廉价的中文字体
10. 金色只作为点缀，不要大面积使用

---

## 16. 给开发 Agent 的一句话任务

请开发一个文旅行业 AI 视频案例展示官网首屏，整体风格为深色电影感、大图背景、金色点缀、右侧案例卡片轮播；案例分为“宣传片”和“广告片”两类，包含“增城文旅”和“南阳汉画馆”案例；需要实现背景联动切换、卡片轮播、分类切换、页码进度条联动、视频弹窗、关于我们与联系合作交互，整体效果参考高端文旅作品集网站。
